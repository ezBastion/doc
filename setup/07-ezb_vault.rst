7/ Vault storage (ezb_vault)
====================
.. image:: /image/under-construction.png

The vault service, store key/value pair in a central store. It's used to not hardcode data in worker's scripts, like password or constant.